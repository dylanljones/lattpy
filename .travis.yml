language: python
python:
  - "3.7"
  - "3.8"
  - "3.9"

before_install:
  - pip install 'pytest>=4.6'
  - pip install pytest-cov
  - pip install codecov
install:
  - pip install -r requirements.txt
  - python setup.py install

#script:
#  - pytest --cov-report term --cov=lattpy -v
#
#after_success:
#  - codecov
